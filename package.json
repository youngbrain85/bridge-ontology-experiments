{
  "name": "bridge-ontology-experiments-ui",
  "version": "0.1.0",
  "private": true,
  "description": "Offline viewer build and browser checks for bridge ontology experiments",
  "scripts": {
    "build:viewer": "node scripts/build-viewer.mjs",
    "test:ui": "node verification/test_ui.mjs"
  },
  "devDependencies": {
    "esbuild": "0.25.10",
    "playwright": "1.55.1",
    "three": "0.180.0"
  }
}
